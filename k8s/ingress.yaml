kind: Ingress
metadata:
  name: insta
  labels:
    app.kubernetes.io/instance: insta
  annotations:
    dev.okteto.com/generate-host: "true"
spec:
  rules:
    - http:
        paths:
          - path: /
            backend:
              serviceName: echo
              servicePort: 80